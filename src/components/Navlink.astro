---
interface Props {
	label: string;
	href: string;
	class?: string;
}
const { label, href, class: className, ...props }: Props = Astro.props;
const isActive = href === Astro.url.pathname;
---

<a href={href} class="">
	{
		isActive ? (
			<strong class="flex">
				&lt;
				<span>{label}</span>&ThinSpace;/&gt;
			</strong>
		) : (
			<span class="flex">{label}</span>
		)
	}
</a>
